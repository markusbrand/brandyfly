## 1. Hillshade Terrain Layer

- [x] 1.1 Add `terrain-hillshade` layer to `assets/map_styles/alpine_relief.json` immediately after the `background` layer, referencing the existing `terrain` raster-dem source with paint properties: `hillshade-illumination-direction: 315`, `hillshade-exaggeration: 0.5`, `hillshade-shadow-color: #3d4659`, `hillshade-highlight-color: #ffffff`, `hillshade-accent-color: #5a7a4a`. Verify: load the style JSON and confirm the `terrain-hillshade` layer is present at index 1 in the layers array, type is `hillshade`, and source is `terrain`.
- [x] 1.2 Verify that `buildStyleJson()` in `maplibre_map_service.dart` correctly strips the `terrain-hillshade` layer when no `terrain.pmtiles` is present (the existing `removeWhere` on line 214-219 already filters `l['type'] == 'hillshade'`). Verify: call `buildStyleJson()` without a regional terrain file and confirm the output JSON contains no `hillshade` layer and no `terrain` source.
- [x] 1.3 Run the app on Android emulator with mock flight mode. Verify: the hillshade layer does not cause errors or blank areas when `terrain.pmtiles` is absent (graceful fallback to flat rendering).

## 2. Map Gesture Fix — Remove GestureDetector and Enable Native Gestures

- [x] 2.1 Remove the `GestureDetector` wrapper around `_buildMapBackground()` in `map_widget.dart` (lines 258-269). Remove the `_panOffset` field and its usage. Remove the `HitTestBehavior.opaque` configuration. Verify: `MapLibreMap` is a direct child of the `Positioned.fill` in the Stack without any gesture interceptor.
- [x] 2.2 Add `gestures: const MapGestures.all()` to the `MapOptions` in `_buildMapBackground()` to explicitly enable pan, pinch-zoom, rotate, and pitch. Verify: `MapOptions` constructor includes the `gestures` parameter.
- [x] 2.3 Add an `onEvent` callback to `MapLibreMap` that listens for `MapEventMoveCamera` and updates a `_mapCamera` state field with the latest `MapCamera`. Also listen for `MapEventStartMoveCamera` to detect user-initiated gesture moves (`CameraChangeReason.apiGesture`) and start the 6-second recenter timer — only for user gestures, not programmatic moves (`developerAnimation`). Verify: log the `_mapCamera` center/zoom on each event and confirm it updates when dragging the map in the emulator.
- [x] 2.4 Refactor `_FlightOverlayPainter` to accept a `MapController` reference and a `MapCamera` instead of the `_panOffset` field. Replace the manual `_toScreen()` method (lines 610-632) with a call to `controller.toScreenLocation()` to batch-convert all geo-coordinates to screen positions. Verify: compile succeeds, the pilot marker and track render at the correct geographic positions after panning.
- [x] 2.5 Update the recenter logic: on `MapEventStartMoveCamera(reason: apiGesture)`, set `_centerOnPilot = false` and start the 6-second `Timer`. On timer expiry, call `controller.animateCamera(center: pilotPosition, nativeDuration: 500ms)` and set `_centerOnPilot = true`. Verify: pan the map, observe it re-centers on the pilot after 6 seconds. Programmatic camera moves (pilot position updates) do not restart the timer.
- [x] 2.6 Update zoom button handlers (`_handleZoom`) to call `controller.animateCamera(zoom: newZoom)` instead of `_mapService.moveCamera()` with the pilot position. Verify: zoom +/- buttons change the map zoom level at the current camera center, not jumping to the pilot position.
- [x] 2.7 Run the app on Android emulator and verify all gesture scenarios: single-finger pan moves both map and overlays in sync; pinch-zoom changes zoom level smoothly with overlays scaling correctly; two-finger rotate changes the bearing. Verify visually that track, airspace, and pilot marker stay geo-locked to the map during all interactions.

## 3. Contour Line Style and Source Configuration

- [x] 3.1 Extend `LocalTileServer` to serve a second PMTiles archive at path `/contours/{z}/{x}/{y}.pbf`. Add a `setContourArchive(String? path)` method mirroring the existing `setPrimaryArchive()` pattern. Verify: unit test or manual check that the tile server responds to both `/tiles/...` and `/contours/...` routes.
- [x] 3.2 In `buildStyleJson()`, add detection of `contours.pmtiles` in the region directory (`$regionDir/contours.pmtiles`). When present, register it with the tile server via `setContourArchive()` and inject a `contours` vector source into the style JSON: `{ "type": "vector", "tiles": ["http://127.0.0.1:<port>/contours/{z}/{x}/{y}.pbf"] }`. When absent, omit the source and remove any contour layers. Verify: with a mock `contours.pmtiles` file, the generated style JSON contains the `contours` source; without it, no `contours` source is present.
- [x] 3.3 Add three contour line layers to `alpine_relief.json` style, positioned after road layers and before label layers: `contour-100m` (line, source `contours`, source-layer `contours`, filter `["==", ["%", ["to-number", ["get", "elevation"]], 100], 0]`, minzoom 10, stroke `#8b6c42` opacity 0.6, width 1.4px), `contour-50m` (filter modulo 50 == 0 but modulo 100 != 0, minzoom 12, opacity 0.35, width 0.9px), `contour-10m` (remaining lines, minzoom 14, opacity 0.2, width 0.6px). Verify: style JSON parses without errors and contains the three contour layers with correct filters and paint properties.
- [x] 3.4 Add elevation label layer for 100m contours: `contour-100m-label` (symbol, source `contours`, source-layer `contours`, filter same as `contour-100m`, minzoom 12, `text-field: "{elevation}m"`, `text-size: 9`, `symbol-placement: "line"`, `text-color: #6b5a3e`, `text-halo-color: #ffffff`, `text-halo-width: 1`). Verify: the label layer is present in the style JSON with correct layout and paint properties.
- [x] 3.5 In `buildStyleJson()`, when contour source/layers are stripped (no `contours.pmtiles`), ensure the `removeWhere` also catches `source == 'contours'` layers. Verify: generate style without contour data and confirm no contour layers remain.

## 4. Contour Visibility Toggle

- [x] 4.1 Wire the existing `showContours` widget parameter to control MapLibre layer visibility. After style load (`onStyleLoaded`), use `StyleController` to set visibility of contour layers based on `showContours`. On `didUpdateWidget`, toggle visibility if `showContours` changed. Verify: with `showContours: false`, contour layers are not visible; toggling to `true` makes them appear (requires contour data to be present).

## 5. Contour Data Pipeline

- [x] 5.1 Add a contour generation step to the pipeline design in `tools/map-pipeline/`: after DEM merge/crop produces the regional GeoTIFF, run `gdal_contour -i 10 -a elevation -f GeoJSON <regional-dem.tif> <contours.geojson>`. Verify: the pipeline script or documentation includes this step with correct parameters.
- [x] 5.2 Add tippecanoe packaging step: `tippecanoe -o contours.mbtiles -z14 -Z10 --simplification=10 --drop-densest-as-needed --layer=contours contours.geojson`. Verify: the step is documented with the correct flags for zoom range, simplification, and layer name.
- [x] 5.3 Add pmtiles conversion step: `pmtiles convert contours.mbtiles contours.pmtiles`. Update the region output structure documentation to include `contours.pmtiles` alongside `map.pmtiles` and `terrain.pmtiles`. Verify: the pipeline documentation shows the complete output structure with all three PMTiles files per region.
- [x] 5.4 Update `tools/map-pipeline/regions.yaml` documentation (or the pipeline README) to reflect the new contour generation step and the expected output file. Verify: documentation is consistent with the actual pipeline steps.

## 6. Integration Validation

- [x] 6.1 Run the app on Android emulator with mock flight mode. Verify end-to-end: map renders without errors; panning moves the map natively; overlays stay geo-synced; recenter timer works; zoom buttons function; hillshade layer definition is present in style (activates when DEM data is available); contour layers are defined but gracefully absent without contour data; no regressions in flight track rendering, airspace overlay, thermal markers, or pilot marker.
- [x] 6.2 Run `dart analyze` on the mobile app. Verify: no new analysis warnings or errors introduced by the changes.
