# Contracts

Versioned cross-module schemas belong here once at least two components consume
them. The bootstrap intentionally defines no product messages or sensor events.

Contract changes require compatibility scenarios and generated-artifact checks.

The first contract added here is the data-source governance record schema used to
track provider evidence, decisions, review timing, licence, privacy, and
operational constraints.
